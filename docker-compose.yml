services:
  url_tests:
    build:
      context: ./url_server
      dockerfile: Dockerfile
    working_dir: /workspace/build
    command: ./tests/run_tests
  
  url_server:
    build:
      context: ./url_server
      dockerfile: Dockerfile
    working_dir: /workspace/build
    command: ./main_app
    ports:
      - "12345:12345"
  
  url_client:
    build:
      context: ./url_server/src/client
      dockerfile: Dockerfile
    depends_on:
      - url_server
    command: ["python3", "main.py", "url_server", "12345"]
    stdin_open: true
    tty: true

  mongodb:
    image: mongo:latest
    container_name: asp_mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: asp_project

  web_server:
    build:
      context: ./web_server
      dockerfile: Dockerfile
    working_dir: /app
    command: npm start
    ports:
      - "3000:3000"
    depends_on:
      - url_server
      - mongodb

  web_app:
    build:
      context: ./web_app
      dockerfile: Dockerfile
    working_dir: /app
    command: serve -s build -l 8080
    ports:
      - "8080:8080"
    depends_on:
      - web_server